import Button from "../button";

const DivUpdateJob = () => {
  return (
    <div>
      <Button>Salvar alterações</Button> {/* esse é verde */}
      <Button graySchema>Excluir</Button>
    </div>
  );
};

export default DivUpdateJob;
