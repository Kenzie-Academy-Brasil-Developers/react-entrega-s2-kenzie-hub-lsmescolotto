import Button from "../button";

const DivUpdateTech = () => {
  return (
    <div>
      <Button>Salvar alteração</Button> {/* esse é verde */}
      <Button graySchema>Excluir</Button>
    </div>
  );
};

export default DivUpdateTech;
