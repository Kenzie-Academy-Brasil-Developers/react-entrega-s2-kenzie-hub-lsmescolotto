import Button from "../../components/button";
import { Container } from "./styles";

const UserProfile = () => {
  return (
    <Container>
      <Button graySchema>Sair</Button>
    </Container>
  );
};

export default UserProfile;
